# Public client configuration only. Never add SUPABASE_SECRET_KEY here.
EXPO_PUBLIC_SUPABASE_URL=https://vqzmzblwmbhmfoikpbhy.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_with_your_key

# Optional Photon-compatible reverse geocoder (public URL, no secret).
EXPO_PUBLIC_GEOCODING_URL=https://photon.komoot.io/reverse

# Optional Photon-compatible place search (public URL, no secret).
EXPO_PUBLIC_PLACE_SEARCH_URL=https://photon.komoot.io/api

# OSRM-compatible driving route service, including /route/v1/driving.
# Public demo by default; use a dedicated endpoint for production volume.
EXPO_PUBLIC_ROUTING_URL=https://router.project-osrm.org/route/v1/driving

# Optional Open-Meteo-compatible endpoint (public URL, no secret).
EXPO_PUBLIC_WEATHER_URL=https://api.open-meteo.com/v1/forecast
